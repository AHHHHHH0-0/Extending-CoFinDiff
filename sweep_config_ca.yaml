name: ca-only-fixed
method: bayes
metric:
  name: best_val_loss
  goal: minimize
parameters:
  learning_rate:
    distribution: log_uniform_values
    min: 1e-5
    max: 1e-4
  base_channels:
    values: [16, 32, 64]
early_terminate:
  type: hyperband
  min_iter: 100
  eta: 3